apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: prompts-microservice-secret
  namespace: statex-apps
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: prompts-microservice-secret
    creationPolicy: Owner
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: secret/prod/prompts-microservice
        property: DB_PASSWORD
