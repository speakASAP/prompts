apiVersion: v1
kind: Service
metadata:
  name: prompts-microservice
  namespace: statex-apps
  labels:
    app: prompts-microservice
    managed-by: k8s-migration
spec:
  type: ClusterIP
  selector:
    app: prompts-microservice
  ports:
    - protocol: TCP
      port: 4750
      targetPort: 4750
      name: http
